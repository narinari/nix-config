name: claude-code
version: 0.1.0
description: "Delegate complex design / architecture / code-review tasks to Claude Code (Opus 4.7) via subprocess. Streams stream-json output back to Hermes and emits per-call cost / session metadata via post_tool_call hook."
author: narinari
provides_tools:
  - claude_code
hooks:
  - post_tool_call
